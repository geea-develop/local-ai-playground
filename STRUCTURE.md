# STRUCTURE_2026_04_12

## Top-Level Directories

- `.git/`: Git configuration files.
- `.github/`: GitHub Actions related files.
- `.vscode/`: VS Code specific settings and configurations.
- `CHANGELOG.md`, `LICENSE.md`, `README.md`, `STRUCTURE.md`: Project documentation files.
- `/assistants/`: Contains assistants-related directories such as `claude-cli`, `goose`, and `graphify`.
- `/backends/`: Contains backends-related directories like `llm_runner` and `localai`.
- `/docs/`: Documentation related directories.
- `/frameworks/`: Contains frameworks-related directories like `deepagents`, `langchain`, `langgraph`, and `smolagents`.
- `/interfaces/`: Contains interfaces-related directories such as `anything_llm` and `dify`.
- `/flowise/`, `/lobe_chat/`, `/onyx/`, `/open_webui/`: Miscellaneous project directories.
